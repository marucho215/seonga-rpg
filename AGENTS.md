# 성아여고 프로젝트 지침

성아여고의 세계관과 캐릭터 사실은 `lore/`를 source of truth로 취급한다.

관련 작업 전에:
- `lore/canon.md`
- `lore/characters/index.md`
- 필요한 `lore/characters/*.md`
- 필요할 경우 `lore/world/factions.md`
를 읽는다.

프로필에 없는 설정을 캐논처럼 추가하지 않는다.
설정이 부족하거나 충돌하면 추측하지 말고 사용자에게 확인한다.

외전, 별도 서사, AI 생성 서사는 `lore/`에 포함되지 않은 한 캐논 근거로 사용하지 않는다.

`game/`의 게임 전용 설정을 원작 캐논으로 자동 승격하지 않는다.

## 정식 게임 제작 기준

이 프로젝트를 프로토타입이나 검증용 최소 구현으로 취급하지 않는다. 실제 완성형 게임을
제작하는 단계로 진행하며, 현재 튜토리얼 사건은 정식 게임의 첫 사건으로 간주한다.

기능이 작동하는 최소 수준에서 멈추지 않는다. 모든 기능과 콘텐츠는 플레이 시간, 선택의 밀도,
캐릭터 존재감, 서사적 완결성, 재플레이 가치, 접근성, 사용성을 함께 고려해 완성도를 높인다.
임시 구현, placeholder, 검증용 축약 구조와 무의미한 선택지는 가능한 한 최종 결과에 남기지 않는다.

필요한 세부 수치, 규칙, 사건, 적, 스킬 효과, 대사, 상황과 연출은 자율적으로 설계할 수 있다.
다만 이미 확정된 핵심 시스템과 기존 캐릭터의 핵심 설정은 유지한다.

현재 게임은 텍스트 기반 표현을 기본 방향으로 삼는다. 사용자에게 별도 승인을 받기 전까지
이미지 생성 도구를 호출하거나 캐릭터 일러스트·초상화·배경·3D 모델·스프라이트·아이콘 등의
새 그래픽 에셋을 생성·다운로드·추가하지 않는다. HTML/CSS/JS 기반 UI와 단순 도형은 허용한다.
그래픽이 필요한 영역은 품질이 낮은 임시 에셋으로 채우지 말고, 텍스트와 코드 기반 UI만으로
완결된 표현을 우선 설계한다.
